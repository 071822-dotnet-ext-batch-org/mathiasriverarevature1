    public static int hurdleRace(int k, List<int> height)
    {
        int maxHurdle = height.Max();
        int potions = 0;
        
            if (k<maxHurdle)
            {
                potions = maxHurdle - k;
            }
            
        Console.WriteLine(potions);
        return potions;
    }

}
